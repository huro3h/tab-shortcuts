# Changelog

このプロジェクトの主な変更点を記録します。

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョニングは [セマンティック バージョニング](https://semver.org/lang/ja/) を採用しています。

新しいバージョンを上に追記します。未リリースの変更は `[Unreleased]` にためていき、
リリース時にバージョン番号と日付へ繰り上げます。

## [Unreleased]

## [1.1.1] - 2026-09-16

### Fixed
- **すべてのタブをリロード**が、シークレットウィンドウのタブを巻き込まないようにした。拡張に「シークレットモードでの実行を許可」を与えている場合、通常ウィンドウから実行するとプライベートセッションのタブまで再読み込みしていた。発動元と同じシークレット状態のタブだけが対象になる。
- **すべてのタブをリロード**が、アクティブなタブを特定できない状況 (切り離した DevTools にフォーカスがあるときなど) で何もせずに終わっていたのを修正。この機能はアクティブタブを使わないため、常に実行される。

## [1.1.0] - 2026-09-16

### Added
- **すべてのタブをリロード** (初期状態では未割り当て): 全ウィンドウのタブをまとめて再読み込みする。Chrome が破棄 (スリープ) したタブは起こさず、ログインや決済のポップアップウィンドウは対象外。

## [1.0.0] - 2026-09-16

### Added
- 初回リリース。以下の機能を含みます。
- **他のタブを閉じる** (`⌥⇧O`): アクティブタブとピン留めタブを残して、同じウィンドウの他のタブを閉じる。
- **右のタブを閉じる** (`⌥⇧R`): アクティブタブより右のタブを閉じる。ピン留めは残す。
- **タブのピン留めを切り替える** (`⌥⇧P`): アクティブタブのピン留めをトグルする。
- **すべてのウィンドウを統合** (初期状態では未割り当て): 他のウィンドウのタブを今見ているウィンドウに集める。タブグループは解体せずそのまま移動し、シークレットウィンドウとポップアップウィンドウは対象外。
- **選択テキストをGoogleで検索 / 前面タブ** (`⌥S`): 選択した文字列を検索し、元のタブの隣で開いて切り替える。
- **選択テキストをGoogleで検索 / 背面タブ** (初期状態では未割り当て): 同上。タブは開くが切り替えない。
- 検索は iframe 内の選択にも対応し、何も選択していないときは空の検索タブを開かない。検索タブは元のタブの隣に開き、閉じると元のタブへ戻る。
